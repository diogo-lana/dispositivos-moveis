import { View, Text } from "react-native"

export default function DashBoard(){

    return (
        <View style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
             <Text style={{fontSize: 30, color: "#e30b0b"}}></Text>
        </View>
    )
}

